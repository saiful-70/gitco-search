/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
};

module.exports = {
  nextConfig: nextConfig,
  images: {
    domains: ["userpic.codeforces.org", "avatars.githubusercontent.com"],
  },
};
